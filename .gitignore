ults/ 文件夹 (把 JSON 和 PNG 拷进去)
mkdir -p results/training_plots
cp logs/baseline/summary.json results/baseline_summary.json
cp logs/relative_pe_only/summary.json results/relative_pe_only_summary.json
# ...等等

# 7. 编辑 README.md (强烈建议)
vim README.md

# 8. 提交并推送！
git add .
git commit -m "feat: Initial commit of transformer project and ablation study"
git push -u origin main

